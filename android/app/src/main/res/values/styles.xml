<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
    </style>


    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="android:background">@drawable/splash</item>
    </style>

    <!-- Тема шита PROCESS_TEXT (A3). windowIsFloating=false + прозрачное окно = контент
         чужого приложения остаётся видимым за затемнением, а шит прижат к низу экрана.
         Диалоговую тему не берём: у неё окно по центру и свой отступ, который ломает
         «прилипание» к нижнему краю. -->
    <style name="Theme.Norsk.BottomSheet" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowIsFloating">false</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowAnimationStyle">@style/Anim.Norsk.BottomSheet</item>
    </style>

    <style name="Anim.Norsk.BottomSheet" parent="@android:style/Animation">
        <item name="android:activityOpenEnterAnimation">@anim/norsk_sheet_in</item>
        <item name="android:activityCloseExitAnimation">@anim/norsk_sheet_out</item>
    </style>
</resources>